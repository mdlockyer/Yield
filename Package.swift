// swift-tools-version:4.2

import PackageDescription

let package = Package(
    name: "Yield",
    products: [
        .library(
            name: "Yield",
            targets: ["Yield"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "Yield",
            dependencies: []),
	]
)
